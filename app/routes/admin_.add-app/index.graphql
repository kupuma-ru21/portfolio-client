mutation CreateApp(
  $title: String!
  $detail: String!
  $link: String!
  $linkType: String!
  $imageUrl: String!
) {
  createApp(
    input: {
      title: $title
      detail: $detail
      link: $link
      linkType: $linkType
      imageUrl: $imageUrl
    }
  )
}
